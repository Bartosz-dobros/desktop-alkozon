# Desktop Alkozon - Requirements File
# =====================================
#
# CODE STYLE SETUP:
# 1. Install all dependencies: pip install -r requirements.txt
# 2. Install pre-commit hooks: pre-commit install
# 3. All commits will automatically check code style (Black, isort, Ruff)
# 4. To manually check all files: pre-commit run --all-files
#

# Core dependencies
flet>=0.25.0
httpx>=0.27.0
pydantic>=2.10
cryptography>=43.0
python-dotenv>=1.0
keyring>=25.0
PyJWT>=2.10

# Development dependencies
pytest>=7.0
pytest-asyncio>=0.21
pytest-mock>=3.10
black>=24.0
isort>=5.13
ruff>=0.8
pre-commit>=3.5
