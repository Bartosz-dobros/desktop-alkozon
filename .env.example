# API Configuration
# Replace with actual API endpoint
API_BASE_URL=https://api.example.com/api
API_TIMEOUT=60 # in seconds

# Application Settings
DEBUG=false
DEMO_MODE=false

# Testing & Development
# To test offline, change API_BASE_URL to http://localhost:1

# Lockout Security Code
# The hard lockout security code (hashed at build time into the .exe).
# Also used at runtime in development (python main.py) when _build_config.py
